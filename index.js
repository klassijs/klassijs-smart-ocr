const { 
  extractText,
  extractLinks,
  makeLinksClickable,
  batchExtract,
  saveLinksToJson
} = require('./src/smartOcr');

module.exports = {
  extractText,
  extractLinks,
  makeLinksClickable,
  batchExtract,
  saveLinksToJson
};
